diff --git a/node_modules/@nestjs/cli/actions/start.action.js b/node_modules/@nestjs/cli/actions/start.action.js
index 42758d9..cc52f2b 100644
--- a/node_modules/@nestjs/cli/actions/start.action.js
+++ b/node_modules/@nestjs/cli/actions/start.action.js
@@ -109,6 +109,15 @@ class StartAction extends build_action_1.BuildAction {
             processArgs.unshift(envFileNodeArgs.join(' '));
         }
         processArgs.unshift('--enable-source-maps');
+        if (options.shell) {
+            // Passing an args array with shell:true triggers DEP0190 on Node >=22.
+            // The args are already quoted for shell use, so join into one command string.
+            const fullCommand = [binaryToRun, ...processArgs].join(' ');
+            return (0, child_process_1.spawn)(fullCommand, {
+                stdio: 'inherit',
+                shell: options.shell,
+            });
+        }
         return (0, child_process_1.spawn)(binaryToRun, processArgs, {
             stdio: 'inherit',
             shell: options.shell,
diff --git a/node_modules/@nestjs/cli/lib/runners/abstract.runner.js b/node_modules/@nestjs/cli/lib/runners/abstract.runner.js
index f8bc139..41c53e8 100644
--- a/node_modules/@nestjs/cli/lib/runners/abstract.runner.js
+++ b/node_modules/@nestjs/cli/lib/runners/abstract.runner.js
@@ -17,7 +17,8 @@ class AbstractRunner {
             shell: true,
         };
         return new Promise((resolve, reject) => {
-            const child = (0, child_process_1.spawn)(`${this.binary}`, [...this.args, ...args], options);
+            const fullCommand = [this.binary, ...this.args, ...args].join(' ');
+            const child = (0, child_process_1.spawn)(fullCommand, options);
             if (collect) {
                 child.stdout.on('data', (data) => resolve(data.toString().replace(/\r\n|\n/, '')));
             }
